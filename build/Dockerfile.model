FROM nvcr.io/nvidia/tritonserver:24.12-py3 as builder

ARG REPOS
ARG MODEL_TYPE
ARG TOKENS

ENV REPOS=$REPOS
ENV REF=$MODEL_TYPE
ENV TOKENS=$TOKENS

ADD . .
RUN pip install transformers Jinja2 huggingface_hub
RUN python3 hf.py